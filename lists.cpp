#include <bits/stdc++.h>
using namespace std;
/*
ARRAY -> SECUENCIAL
LISTAS ENLAZADAS -> DISPERSAS

Linear Search
Binary Search (ordenado)
*/
template<typename T>
struct Node {
    T data;
    Node<T> *next = nullptr, *prev = nullptr;

    // insert -> "optimizado"   
};
